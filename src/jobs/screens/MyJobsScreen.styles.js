import { StyleSheet } from 'react-native';

export const getStyles = (theme) => StyleSheet.create({
  // Example: Add your extracted styles here
  // Replace all theme references with the function argument
  // For each inline style, create a named style here and use it in the main file
  // Example:
  // container: {
  //   flex: 1,
  // },
}); 